<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background: #fee2e2;
        border-left: 4px solid #dc2626;
        padding: 20px;
        margin-bottom: 20px;
      }
      .header h1 {
        margin: 0 0 10px 0;
        color: #991b1b;
        font-size: 24px;
      }
      .content {
        background: #f9fafb;
        padding: 20px;
        border-radius: 8px;
        margin-bottom: 20px;
      }
      .score-card {
        background: white;
        padding: 15px;
        border-radius: 6px;
        margin: 15px 0;
        border: 1px solid #e5e7eb;
      }
      .score-value {
        font-size: 32px;
        font-weight: bold;
        color: #dc2626;
      }
      .components {
        background: #fef2f2;
        padding: 10px;
        border-radius: 4px;
        margin-top: 10px;
        font-size: 14px;
      }
      .cta-button {
        display: inline-block;
        background: #2563eb;
        color: white;
        padding: 12px 24px;
        text-decoration: none;
        border-radius: 6px;
        font-weight: 600;
        margin-top: 15px;
      }
      .cta-button:hover {
        background: #1d4ed8;
      }
      .footer {
        margin-top: 30px;
        padding-top: 20px;
        border-top: 1px solid #e5e7eb;
        font-size: 14px;
        color: #6b7280;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>🚨 Low First Session Quality Alert</h1>
      <p><strong>Tutor:</strong> <%= @tutor.name %> (<%= @tutor.email %>)</p>
      <p><strong>Alert Triggered:</strong> <%= @alert.triggered_at.strftime('%B %d, %Y at %I:%M %p') %></p>
    </div>

    <div class="content">
      <h2>First Session Quality Score (FSQS)</h2>
      <div class="score-card">
        <div class="score-value"><%= @fsqs_score %></div>
        <p style="margin: 5px 0 0 0; color: #6b7280;">out of 100 (Higher = Better Quality)</p>
      </div>

      <p><strong>Why this matters:</strong> This tutor's recent first session with a student showed concerning patterns that could impact student retention and experience.</p>

      <% if @score_components.any? %>
        <div class="components">
          <strong>Score Breakdown:</strong>
          <ul style="margin: 10px 0 0 0; padding-left: 20px;">
            <% @score_components.each do |key, value| %>
              <li><%= key.to_s.humanize %>: <%= value %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <h3>Recommended Actions</h3>
      <ul>
        <li>Review the session transcript for specific patterns</li>
        <li>Schedule a coaching conversation with <%= @tutor.name %></li>
        <li>Share best practices for first sessions (goal-setting, encouragement, clear closing)</li>
        <li>Monitor next few sessions for improvement</li>
      </ul>

      <a href="<%= @admin_dashboard_url %>" class="cta-button">View Admin Dashboard →</a>
    </div>

    <div class="footer">
      <p>This is an automated alert from the Tutor Quality Scoring System.</p>
      <p>To manage alert settings or unsubscribe, visit your admin dashboard.</p>
    </div>
  </body>
</html>

